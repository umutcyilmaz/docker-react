sudo: required
services:
  - docker

before_install:
  - docker build -t umutcyilmaz/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true umutcyilmaz/docker-react bash -c "npm install && npm run test -- --coverage"
